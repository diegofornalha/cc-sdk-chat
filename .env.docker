# 🐳 Configuração Docker - Claude Code SDK Chat

# === PORTAS DOS SERVIÇOS ===
API_PORT=8992
FRONTEND_PORT=3082
PROXY_PORT=80

# === CONFIGURAÇÕES DA API ===
LOG_LEVEL=INFO
MAX_SESSION_TIME=3600
MAX_SESSIONS_PER_IP=10

# === CONFIGURAÇÕES CLAUDE CODE ===
# ⚠️ ATENÇÃO: Essas configurações removem todas as confirmações de segurança
# Use apenas em ambientes controlados e confiáveis
CLAUDE_DANGEROUSLY_SKIP_PERMISSIONS=true
CLAUDE_AUTO_APPROVE=true
CLAUDE_TRUST_ALL_DIRECTORIES=true
CLAUDE_AUTO_APPROVE_MCP=true
CLAUDE_BYPASS_PERMISSIONS=true
CLAUDE_NO_PROMPT=true
CLAUDE_DISABLE_TELEMETRY=1

# === CONFIGURAÇÕES DO CONTAINER ===
# UID do usuário (deve coincidir com o host para volumes)
CONTAINER_UID=1002
CONTAINER_GID=1002

# === CONFIGURAÇÕES DE REDE ===
# Interno: comunicação entre containers
NEXT_PUBLIC_API_URL=http://api:8992

# === CONFIGURAÇÕES DE PRODUÇÃO ===
NODE_ENV=production
DOMAIN=suthub.agentesintegrados.com

# Configurações SSL (para produção com HTTPS)
SSL_CERT_PATH=/etc/nginx/ssl/cert.pem
SSL_KEY_PATH=/etc/nginx/ssl/key.pem

# Configurações específicas do Docker
# Network interno do Docker
DOCKER_NETWORK=cc-sdk-chat-network

# URLs internas para comunicação entre serviços
API_INTERNAL_URL=http://api:8989
FRONTEND_INTERNAL_URL=http://frontend:3082

# Configurações de volumes
SESSIONS_VOLUME_PATH=/app/sessions
LOGS_VOLUME_PATH=/app/logs

# Configurações de healthcheck
HEALTHCHECK_INTERVAL=30s
HEALTHCHECK_TIMEOUT=10s
HEALTHCHECK_START_PERIOD=5s
HEALTHCHECK_RETRIES=3

# Configurações de restart policy
RESTART_POLICY=unless-stopped

# Configurações de resource limits (opcional)
# API_MEMORY_LIMIT=512m
# API_CPU_LIMIT=0.5
# FRONTEND_MEMORY_LIMIT=256m
# FRONTEND_CPU_LIMIT=0.3